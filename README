# WAWay Lite

WaWay Lite is a lightweight whatsapp api gateway

## Installing

To build this run this app

1. copy .env.example file to .env file
2. modify your .env based on your setting
3. installing required depedency by run `npm install`
4. to serve your app run this code `npm run dev`
5. build your app for production by run `npm run build`
